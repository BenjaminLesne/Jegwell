{
  "scripts": {
    "tests": "cd tests/selenium; python -m unittest; cd -; ./vendor/bin/phpunit --verbose --debug --coverage-html tests/PHPunit/coverage tests/PHPunit/tests",
    "single_test": "cd tests/selenium; python -m unittest $FILE; cd -;"
  },
  "devDependencies": {
    "@types/node": "^18.8.2",
    "@types/stripe-v3": "^3.1.27",
    "typescript": "^4.8.4"
  },
  "dependencies": {
    "@sentry/browser": "^7.14.1",
    "@sentry/tracing": "^7.14.1",
    "dotenv": "^16.0.3"
  }
}
