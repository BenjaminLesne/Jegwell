{
  "scripts": {
    "tests": "cd tests/selenium; python -m unittest; cd -",
    "single_test": "cd tests/selenium; python -m unittest $FILE; cd -;"
  },
  "devDependencies": {
    "@types/node": "^18.8.2",
    "typescript": "^4.8.4"
  },
  "dependencies": {
    "@sentry/browser": "^7.14.1",
    "@sentry/tracing": "^7.14.1",
    "dotenv": "^16.0.3"
  }
}
