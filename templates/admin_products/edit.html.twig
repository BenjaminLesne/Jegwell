
{% extends 'base.html.twig' %}

{% block title %}Hello AdminProductController and the edit function!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Editer le produit</h1>
{{ form_start(form) }}
{{ form_widget(form) }}

{# <ul class=""
    data-index="{{ form.options|length > 0 ? form.options|last.vars.name + 1 : 0 }}"
    data-prototype="{{ form_widget(form.options.vars.prototype)|e('html_attr') }}"
></ul> #}
<button>editer</button>
{{ form_end(form)}}

</div>
{% endblock %}
